# frozen_string_literal: true

RSpec.describe Trip2fizzbuzz do
  it "has a version number" do
    expect(Trip2fizzbuzz::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
